validate_catalog.py — proof run
date(UTC): 2026-08-30T13:41:30Z
commit: ff3038c

############ docs/client/fixtures/catalog_good.csv
بررسی فایل: docs/client/fixtures/catalog_good.csv
تعداد سطرهای بررسی‌شده: ۳
------------------------------------------------------------

[هشدار] ۱ مورد — اختیاری:

  ! تعداد محصولات ۳ است. برای اینکه پیشنهادها معنادار باشند دست‌کم ۵۰ محصول توصیه می‌شود.
------------------------------------------------------------
نتیجه: فایل قابل قبول است. هشدارها اجباری نیستند.
EXIT=0

############ docs/client/fixtures/catalog_broken.csv
بررسی فایل: docs/client/fixtures/catalog_broken.csv
تعداد سطرهای بررسی‌شده: ۴
------------------------------------------------------------

[خطا] ۱۵ مورد — باید اصلاح شود:

  ✕ سطر ۳ · ستون «شناسه»: شناسهٔ «chair-001» تکراری است؛ پیش‌تر در سطر ۲ آمده.
  ✕ سطر ۳ · ستون «دسته»: مقدار «armchair» مجاز نیست. مقادیر مجاز: chair · coffee_table · decor · lighting · rug · sofa · storage
  ✕ سطر ۳ · ستون «لینک فروشنده»: باید با https:// شروع شود (نه http://).
  ✕ سطر ۴ · ستون «قیمت»: «4,500,000 تومان» عدد صحیح نیست. فقط رقم بنویسید، بدون کاما و بدون کلمهٔ تومان (مثلاً 24500000).
  ✕ سطر ۴ · ستون «ابعاد»: قالب درست «طول×عرض×ارتفاع» به سانتی‌متر است، مثلاً 220x95x85.
  ✕ سطر ۴ · ستون «رنگ‌ها»: «#GGGGGG» کد رنگ معتبر نیست. قالب درست شش‌رقمی است، مثلاً #2D8C50.
  ✕ سطر ۴ · ستون «سبک»: مقدار «rustic» مجاز نیست. مقادیر مجاز: boho · classic · industrial · minimal · modern · scandinavian
  ✕ سطر ۵ · ستون «شناسه»: فقط حروف انگلیسی، عدد، خط تیره و زیرخط مجاز است (مثلاً sofa-014).
  ✕ سطر ۵ · ستون «نام فارسی»: خالی است و باید پر شود.
  ✕ سطر ۵ · ستون «فروشنده»: خالی است و باید پر شود.
  ✕ سطر ۵ · ستون «نوع فضا»: مقدار «bedroom» مجاز نیست. فعلاً فقط living_room پشتیبانی می‌شود.
  ✕ سطر ۵ · ستون «قیمت»: قیمت ۹۵۰۰۰۰۰۰۰۰۰ تومان بیش از حد بزرگ است. شاید اشتباهی صفر اضافه شده باشد.
  ✕ سطر ۵ · ستون «نشانی تصویر»: خالی است و باید پر شود.
  ✕ سطر ۵ · ستون «ابعاد»: خالی است و باید پر شود.
  ✕ سطر ۵ · ستون «جنس»: مقدار «ceramic» مجاز نیست. مقادیر مجاز: fabric · leather · metal · rattan · wood

[هشدار] ۲ مورد — اختیاری:

  ! سطر ۵ · ستون «توصیف»: خیلی کوتاه است. توصیف دقیق‌تر، پیشنهادهای بهتری تولید می‌کند.
  ! تعداد محصولات ۴ است. برای اینکه پیشنهادها معنادار باشند دست‌کم ۵۰ محصول توصیه می‌شود.
------------------------------------------------------------
نتیجه: فایل آمادهٔ تحویل نیست. موارد بالا را اصلاح و دوباره اجرا کنید.
EXIT=1

############ docs/client/catalog-template.csv
بررسی فایل: docs/client/catalog-template.csv
تعداد سطرهای بررسی‌شده: ۲
------------------------------------------------------------

[هشدار] ۱ مورد — اختیاری:

  ! تعداد محصولات ۲ است. برای اینکه پیشنهادها معنادار باشند دست‌کم ۵۰ محصول توصیه می‌شود.
------------------------------------------------------------
نتیجه: فایل قابل قبول است. هشدارها اجباری نیستند.
EXIT=0

############ cross-check: real 150-product seed catalog converted to CSV
بررسی فایل: /tmp/real150.csv
تعداد سطرهای بررسی‌شده: ۱۵۰
------------------------------------------------------------

[هشدار] ۱ مورد — اختیاری:

  ! ۱۳۳ محصول تصویر تکراری دارند. تصویر اختصاصی برای هر محصول نتیجهٔ بهتری می‌دهد.
------------------------------------------------------------
نتیجه: فایل قابل قبول است. هشدارها اجباری نیستند.
EXIT=0

############ edge cases
/tmp/nocol.csv -> EXIT=1
/tmp/empty.csv -> EXIT=1
/tmp/bad_enc.csv -> EXIT=1
/tmp/blank.csv -> EXIT=0
/tmp/definitely_missing.csv -> EXIT=1
