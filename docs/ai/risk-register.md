# AI & Data Risk Register — Stage 04

Owner: AI Recommendation Engine, Data Quality & MLOps stage (Master Prompt 04).
Branch: `arena/01a02613-smart-interior-decor-recommend` · Date: 2026-08-21.
Scoring: Likelihood / Impact are Low–Critical (qualitative); **Residual** is the
rating after the controls listed in Treatment are in place. Companion:
[`docs/security/RISK_REGISTER.md`](../security/RISK_REGISTER.md) (platform risks).

| ID | Risk | Inherent (L/I) | Treatment on this branch | Residual | Verification |
|---|---|---|---|---|---|
| **AI-01** | Silent hash-embedding fallback in Production: `get_backend()` degraded CLIP→hash with only a log line, so recommendations would run on fake geometry without any operator knowing | High / **Critical** | `ai/embedding_service.py` raises `EmbeddingBackendError` in production when CLIP cannot load **and** when `EMBEDDING_BACKEND=hash` is configured; `validate_embedding_runtime()` exported for startup wiring (IR-AI-001) | Low — requires an operator to also delete the guard | `tests/test_embedding_guards.py`, `docs/agent-reports/ai-recommender-evidence/` |
| **AI-02** | Extraction failure falls back to keyword-mock features that look real; a network blip during admin upload could store fabricated taxonomy values | Medium / High | Fallback result is stamped `provider="mock-fallback"` + `provider_error`, confidence capped at 0.3 and `needs_review=true`; **in production no mock inference at all** — empty features + provider error | Low | `tests/test_extraction_review.py` |
| **AI-03** | Low-confidence extraction enters `is_verified=False` limbo but nothing surfaces it — products wait forever, or get verified without scrutiny | Medium / Medium | `review_decision()` with explicit thresholds (`AUTO_ACCEPT≥0.80`, review below); `needs_review` + machine-readable `review_reasons` stored in `extraction_raw`; admin queue wiring requested via IR-AI-002 | Medium — UI/queue is another owner | `tests/test_extraction_review.py`, IR-AI-002 |
| **AI-04** | Mock benchmark numbers presented as vision-model accuracy (the historic "100%" extraction claim) | High / **Critical** (honesty) | Evaluation harness labels mode `MOCK`/`REAL:<provider>` in stdout, JSON and reports; CI mock run is a harness baseline only; real run without a key exits BLOCKED | Low | `docs/ai/evaluation-report.md`, evidence logs |
| **AI-05** | Unversioned AI stack: prompt/model/taxonomy/embedding drift makes quality regressions unexplainable | Medium / Medium | `ai/model_registry.py` stamps provider, model, prompt version, taxonomy version, embedding model+dim, recommender config version into results and evidence | Low | `tests/test_embedding_guards.py::TestVersionIdentity`, extraction result stamps in `tests/test_extraction_review.py` |
| **AI-06** | Embedding dimension mismatch (model or config change) corrupts pgvector column silently | Low / High | `verify_embedding()` checks dim + unit norm + NaN; pgvector column rejects wrong-dim inserts; re-embedding/version-migration strategy documented | Low | `tests/test_embedding_guards.py`, `docs/ai/model-versions.md` |
| **AI-07** | Weights appear learned: `WEIGHTS` constants imply data-driven tuning that never happened | Medium / Medium (trust) | Weights moved to versioned `recommender_config.json` with explicit `weights_source: heuristic (ADR-005)`, not learned; validated at import | Low | `docs/ai/recommender-config.md` |
| **AI-08** | Non-deterministic ranking on score ties makes A/B tests and user trust impossible | Medium / Low | Tie-break on stable product `id` at every sort (Stage B, Stage C, feedback re-rank, SQL `ORDER BY`) | Low | `tests/test_recommender_v2.py` |
| **AI-09** | All-top-5 from one style / near-duplicate listings crowd out variety | Medium / Low | Duplicate suppression (normalized title + embedding cosine) and per-style cap in the final cut, versioned and configurable | Low | `tests/test_recommender_v2.py` |
| **AI-10** | No-result queries look like bugs (empty 200 with no context); out-of-budget items leak in via "helpful" fallbacks | Medium / Medium | Explicit `meta.empty_categories` + `meta.query` echo; hard budget filter retained (no padding); tests for both | Low | `tests/test_recommender_v2.py` |
| **AI-11** | Explanation numbers drift from actual score components | Medium / High (regulatory-adjacent) | Explanation values are computed in the same function as the score; fidelity tests recompute `final_score` from explanation × weights; weights version stamped in payload | Low | `tests/test_recommender_v2.py` |
| **AI-12** | Post-filtered ANN recall loss at catalog scale (ef_search default 40) | Medium / High | Existing `HNSW_EF_SEARCH=400` retained; recall re-measured at 1k/10k in the benchmark with query plans | Medium — staging hardware differs | `docs/agent-reports/ai-recommender-evidence/` |
| **AI-13** | User images / quiz answers sent to a foreign provider without a documented data flow, retention or authorization | Medium / High (privacy) | Privacy & cost assessment: provider, purpose, retention, egress (Iran→US consideration for a Persian catalog), authorization gate before any real benchmark run | Medium — legal sign-off is external | `docs/ai/privacy-cost-assessment.md` |
| **AI-14** | AI cost blow-up (admin upload loop, benchmark reruns) | Medium / Medium | Per-admin upload rate limit (existing), benchmark `--sample`, cost model with monthly budget estimate; real runs require key + documented authorization | Low | `docs/ai/privacy-cost-assessment.md` |
| **AI-15** | Feedback events oversold as "trained recommender" | Low / Medium (honesty) | Feedback doc states clearly: bounded heuristic re-rank today; event schema for click/save designed but **not** a learned model | Low | `docs/ai/feedback-events.md` |
| **AI-16** | Taxonomy drift: extractor allowlists duplicated across modules, Persian labels unmaintained, no stable IDs | Medium / Medium | Single taxonomy module + validated JSON (`taxonomy_version`, unique stable IDs, `name_fa` mandatory); model `CATEGORIES` cross-checked against taxonomy in tests | Low | `tests/test_ai_taxonomy.py` |
| **AI-17** | Unknown quiz values (patterns/colors outside taxonomy, malformed hex) silently degrade scoring | Medium / Low | Schema validators for patterns (taxonomy allowlist) and colors (hex form) with clear 422s; empty lists remain the explicit "no preference" neutral | Low | `tests/test_ai_taxonomy.py` |
| **AI-18** | `GeminiProvider` downloads the image server-side with no SSRF validation (deferred risk D-03 / IR-SEC-003) | Medium / High | `validate_public_url(resolve=True)` before fetch + per-hop revalidation on manual redirect following (same pattern as the link checker) — **closes IR-SEC-003 within this stage's ownership** | Low | `tests/test_extraction_review.py` |

## Accepted residuals

| ID | Residual | Why accepted | Review trigger |
|---|---|---|---|
| AR-1 | Real vision-model quality unknown until a key exists | Cannot be fabricated; mock is labelled; human review is the default gate for all extraction | First key provisioning; then rerun `evaluate_extraction.py --real` |
| AR-2 | Real CLIP verification depends on a ~600 MB model download | Sandbox/CI install `sentence-transformers` on demand; hash backend remains dev/test-only | Production embedding bootstrap runbook (docs/ai/model-versions.md) |
| AR-3 | p95 numbers measured on sandbox pgserver, not staging hardware | Declared environment in every latency table; staging rerun is a deployment task | Staging deployment |
| AR-4 | Feedback model stays heuristic | Learning from feedback needs volume and an evaluation set that does not exist yet; bounded re-rank is transparent | ≥ ~10k feedback events |
