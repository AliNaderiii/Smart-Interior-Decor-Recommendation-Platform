# Core
fastapi>=0.115
uvicorn[standard]>=0.30
orjson>=3.10           # fast JSON serialisation for /recommend (V2 Phase 2)
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1

# Database
sqlalchemy>=2.0.30
alembic>=1.13
pgvector>=0.2.5
psycopg[binary]>=3.1; platform_system != "Windows"

# Auth & crypto
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt<4.1
cryptography>=42

# Cache
redis>=5.0
fakeredis>=2.23

# Storage & HTTP
boto3>=1.34
httpx>=0.27
python-multipart>=0.0.9

# AI (CLIP is optional — hash fallback works offline; uncomment for CLIP)
numpy>=1.26
pillow>=10.3
# sentence-transformers>=3.0
# torch>=2.3

# Testing
pytest>=8.2
pytest-asyncio>=0.23
pytest-benchmark>=4.0
